<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme_LoaidngDialog" parent="@android:style/Theme.Dialog">
        <!--Dialog的windowFrame框为无 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否漂现在activity上-->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!--是否有标题栏-->
        <item name="android:windowNoTitle">true</item>
        <!-- dim:模糊的 阴影效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 背景图片的大小也影响窗口的大小 -->
        <item name="android:windowBackground">@color/color_transparent</item>
    </style>

    <style name="Theme_CustomProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/progress_loading</item>
        <item name="android:minWidth">25dp</item>
        <item name="android:minHeight">25dp</item>
        <item name="android:maxWidth">50dp</item>
        <item name="android:maxHeight">50dp</item>
    </style>

</resources>